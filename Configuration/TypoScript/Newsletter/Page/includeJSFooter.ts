page {
 		// empty statement for proper comments only
  includeJSFooter {
	}
		// startNewsletterPopupExitIntent
  includeJSFooter =
  includeJSFooter {
			// if.isTrue = $start.newsletterpopup.exitintent.page (here: {$start.newsletterpopup.exitintent.page})
    startNewsletterPopupExitIntent = {$start.newsletterpopup.exitintent.js.path}
    startNewsletterPopupExitIntent {
				// if.isTrue = $start.newsletterpopup.exitintent.page (here: {$start.newsletterpopup.exitintent.page})
			if =
			if {
				isTrue = {$start.newsletterpopup.exitintent.page}
			}
    }
	}
}
