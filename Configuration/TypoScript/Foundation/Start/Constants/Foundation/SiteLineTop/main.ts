start {
  foundation {
    sitelinetop {
      # cat=start - foundation - sitelinetop//100;  type=string;                 label= Outer wrap class:The class of the sitelinetop wrap
      outerwrapclass = siteLineTop siteLineTopNavigation hide-for-small-only
    }
  }
}