start {
  foundation {
		newsletter {
			# cat=start - foundation - newsletter//100;  type=boolean; label= PopUp: Display a popup (modal) for subscribing to the newsletter. Has an effect only, if start pages newsletter is set.
			modalEnabled		= 0
			# cat=start - foundation - newsletter//102;  type=string; label= PopUp header tag: Tag like h1, h2, h3, ...
			modalHeaderTag	= h1
		}
  }
}