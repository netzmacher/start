	// empty statement for proper comment only
lib {
}
  // start: contentElement
lib =
lib {
		// empty statement for proper comment only
	contentElement {
	}
		// start: templateRootPaths, partialRootPaths, layoutRootPaths
	contentElement = FLUIDTEMPLATE
	contentElement {
			// empty statement for proper comment only
		templateRootPaths {
		}
			// start: 178
		templateRootPaths =
		templateRootPaths {
				// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}: EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Templates/
			178 = 
			178 {
					// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}
				cObject = TEXT
				cObject {
						// isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					if =
					if {
						isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					}
					value = EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Template/
				}
			}
		}
			// empty statement for proper comment only
		partialRootPaths {
		}
			// start: 178
		partialRootPaths =
		partialRootPaths {
				// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}: EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Partials/
			178 = 
			178 {
					// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}
				cObject = TEXT
				cObject {
						// isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					if =
					if {
						isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					}
					value = EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Partials/
				}
			}
		}
			// empty statement for proper comment only
		layoutRootPaths {
		}
			// start: 178
		layoutRootPaths =
		layoutRootPaths {
				// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}: EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Layout/
			178 = 
			178 {
					// if.isTrue = {$start.bootstrap.base.includeDefaultTemplate}
				cObject = TEXT
				cObject {
						// isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					if =
					if {
						isTrue = {$start.bootstrap.base.includeDefaultTemplate}
					}
					value = EXT:start/Resources/Private/Templates/FluidStyledContent/Bootstrap/Layouts/
				}
			}
		}
	}
}