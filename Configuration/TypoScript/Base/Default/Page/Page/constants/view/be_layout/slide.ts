start {

  # cat = start - view - be_layout - slide//100; type=options[0,-1,1,2]; label= Slide: If set and no content element is found by the select command, the rootLine will be traversed back until some content is found. Possible values are -1 (slide back up to the siteroot / recommended), 1 (only the current level) and 2 (up from one level back).
  fluid.path.be_layout.slide = -1
}