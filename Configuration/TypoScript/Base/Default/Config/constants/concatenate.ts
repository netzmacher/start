start {

  concatenate {
    # cat=start - concatenate css and jss//100; type=boolean; label= CSS:If set, CSS files will concatenated. This property will enabled: config.concatenateCss.
    css = 1
    # cat=start - concatenate css and jss//101; type=boolean; label= JSS:If set, JSS files will concatenated. This property will enabled: config.concatenateJs.
    jss = 1
  }

}