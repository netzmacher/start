start {
  ce {
	  teasercard {
			css {
				# cat=start - content element - teasercard - 0//100; type=string;  label= Outer wrap class:Additional class for the outer wrap. Usually empty.
				plusouterwrap	= 
				# cat=start - content element - teasercard - 0//201; type=string;  label= Panel class:Class for a panel / a box. Usually: panel.
				panel					= panel
				# cat=start - content element - teasercard - 0//301; type=string;  label= Button class:Class for a link outside the panels. I.e: button round tiny alert
				button				= button
				# cat=start - content element - teasercard - 0//302; type=string;  label= Button class:Class for a link in a panel. I.e: button round tiny alert
				panelbutton		= button
			}
	  }
  }
}