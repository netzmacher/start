xxtt_content {
		// empty statement for proper comments only
	stdWrap {
	}
		// start (e-mail): innerWrap.cObject.default
	stdWrap =
	stdWrap {		
			// empty statement for proper comments only
		innerWrap {
		}
			// start (e-mail): cObject.default
		innerWrap =
		innerWrap {
				// empty statement for proper comments only
			cObject {
			}
				// start (e-mail): default
			cObject = CASE
			cObject {
					// empty statement for proper comments only
				default {
				}
					// start (e-mail): 10.cObject.default.value, 30.cObject.default.value
				default = COA
				default {
					10.cObject.default.value = <tr><th><div id="c{field:uid}"
					30.cObject.default.value = >|</div></th><th class="expander"></th></tr>
				}
			}
		}
  }
}
